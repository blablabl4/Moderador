# WPPConnect Server - Built from latest source
# This builds an up-to-date version that supports current WhatsApp Web QR codes
FROM node:20-slim

# Install Chromium + SSL certificates + git (all in one layer)
RUN apt-get update && apt-get install -y \
    chromium \
    ca-certificates \
    git \
    fonts-ipafont-gothic fonts-wqy-zenhei fonts-thai-tlwg fonts-kacst fonts-freefont-ttf \
    --no-install-recommends && \
    rm -rf /var/lib/apt/lists/*

ENV PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
ENV PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

WORKDIR /usr/src/wpp-server

# Clone and build latest wppconnect-server
RUN git clone --depth 1 https://github.com/wppconnect-team/wppconnect-server.git . && \
    npm install && \
    npm run build

EXPOSE 8080

CMD ["node", "dist/server.js"]
